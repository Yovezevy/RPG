#pragma once

constexpr int   WIN_W        = 800;
constexpr int   WIN_H        = 600;
constexpr float PLAYER_SPEED = 140.f;   // pixel per detik
constexpr float ANIM_FPS     = 8.f;     // kecepatan animasi jalan

enum class State { Explore, Dialog, Battle };
